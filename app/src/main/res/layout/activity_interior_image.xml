<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_interior_image"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    tools:context="com.interiordesign.interiordesign.InteriorImageActivity">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="0dp"
        android:layout_height="300dp"
        android:scaleType="fitXY"
        android:src="@drawable/hall_images"
        app:layout_constraintBottom_toBottomOf="@+id/activity_interior_image"
        app:layout_constraintLeft_toLeftOf="@+id/activity_interior_image"
        app:layout_constraintRight_toRightOf="@+id/activity_interior_image"
        app:layout_constraintTop_toTopOf="@+id/activity_interior_image" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="60dp"
        android:layout_height="40dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:layout_marginLeft="20dp"
        app:layout_constraintTop_toBottomOf="@+id/imageView" />

    <ImageView
        android:id="@+id/decorator"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:src="@drawable/chandelier_1"
        app:layout_constraintBottom_toBottomOf="@+id/fab" />

</android.support.constraint.ConstraintLayout>
